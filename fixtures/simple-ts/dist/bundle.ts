// Generated build artifact. The fixture .gitignore excludes dist/, so this
// file must never show up in walk or chunk results.
export function shouldNotAppear(): void {
  throw new Error("spelunk indexed an ignored file");
}
